Actually, it would make more sense to keep this bug open 
(but deferred to a future release) for a proper reversals test,
and open another ticket to tick off the partial functionality
of multi-suite means.
