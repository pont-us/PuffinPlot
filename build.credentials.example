# This is a template for the build.credentials.properties file, which is
# required for the ant upload target, which uses the ant-googlecode task.

# Google Code username. This is usually your gmail username, excluding the
# "@gmail.com" part.
gc.username=myusername

# Google Code generated password. This is not the same as your gmail
# password. It's the randomly generated password used for VCS commits, which
# can be found by logging into Google and going to
# https://code.google.com/hosting/settings .
gc.password=mypassword
