Note also that GA's next sentence commences
‘I noticed in the PuffinPlot manual that...’
which shows that (if indeed this bug is due to misunderstanding
the GUI) the user did read the manual -- i.e. it's
an ‘unclear documentation’ problem rather than a 
‘user didn't bother reading the documentation’ problem.
